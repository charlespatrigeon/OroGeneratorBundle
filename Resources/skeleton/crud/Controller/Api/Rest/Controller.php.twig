<?php

namespace {{ namespace }}\Controller\Api\Rest;

use FOS\RestBundle\Controller\Annotations\NamePrefix;
use FOS\RestBundle\Controller\Annotations\RouteResource;
use Oro\Bundle\SecurityBundle\Annotation\Acl;
use Oro\Bundle\SoapBundle\Controller\Api\Rest\RestController;

/**
 * @RouteResource("{{ entity|lower }}")
 * @NamePrefix("{{ routePrefix }}_api_")
 */

class {{ entity }}Controller extends RestController
{
    /**
     * @Acl(
     *      id="{{ vendor }}.{{ entity|lower }}.delete",
     *      type="entity",
     *      class="{{ shortcutEntity }}",
     *      permission="DELETE"
     * )
     */
    public function deleteAction($id)
    {
        return $this->handleDeleteRequest($id);
    }

    public function getForm()
    {
    }

    public function getFormHandler()
    {
    }

    public function getManager()
    {
        return $this->get('{{ vendor }}.{{ entity|lower }}_manager.api');
    }
}